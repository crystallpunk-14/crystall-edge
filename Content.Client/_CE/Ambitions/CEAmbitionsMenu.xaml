<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      Title="{Loc 'ce-ambitions-ui-menu-title'}"
                      MinSize="200 100"
                      SetSize="450 515">
    <BoxContainer Orientation="Vertical"
                  HorizontalExpand="True"
                  VerticalExpand="True">
        <RichTextLabel Margin="12" StyleClasses="highlight" HorizontalAlignment="Center" Text="{Loc 'ce-ambitions-ui-menu-guide'}"/>
        <PanelContainer VerticalExpand="True" Margin="10 10 10 10">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#1B1B1E"/>
            </PanelContainer.PanelOverride>
            <ScrollContainer
                HScrollEnabled="False"
                HorizontalExpand="True"
                VerticalExpand="True">
                <BoxContainer
                    Orientation="Vertical"
                    VerticalExpand="True"
                    Margin="10 10 10 10">
                    <BoxContainer
                        Name="AmbitionsContainer"
                        Orientation="Vertical"
                        VerticalExpand="True"
                        Margin="10 10 10 0">
                    </BoxContainer>
                    <BoxContainer
                        Orientation="Horizontal"
                        Margin="10 10 10 0">
                        <Button Name="AddAmbitionButton"
                                Text="{Loc 'ce-ambitions-buttons-add'}"
                                StyleClasses="OpenRight"
                                HorizontalExpand="True"
                                Access="Public"
                                Disabled="False" />
                        <Button Name="LockAmbitionButton"
                                Text="{Loc 'ce-ambitions-buttons-lock'}"
                                StyleClasses="OpenLeft"
                                Access="Public"
                                Disabled="False" />
                    </BoxContainer>
                </BoxContainer>
            </ScrollContainer>
        </PanelContainer>
    </BoxContainer>
</controls:FancyWindow>
